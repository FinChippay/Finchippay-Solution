FROM node:22.23.2-alpine

WORKDIR /app

# Install dependencies first (layer cache)
COPY backend/package*.json ./
RUN npm ci

# Copy shared module
COPY shared/ ./shared/

# Source is mounted via volume at runtime — no COPY needed for backend/
EXPOSE 4000

# nodemon watches for changes and restarts automatically
CMD ["npm", "run", "dev"]
